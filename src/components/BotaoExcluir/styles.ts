import styled from 'styled-components';
import variaveis from '../../styles/variaveis';

export const BotaoExcluirStyled = styled.button`
    all: unset;
    padding: 1dvh;
    border-radius: 6px 0 6px 0;
    color: ${variaveis.corH1};
`;
