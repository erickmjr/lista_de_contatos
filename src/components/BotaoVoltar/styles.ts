import { Link } from 'react-router-dom';
import styled from 'styled-components';

export const Retangulo = styled(Link)`
    all: unset;
    font-size: 2dvh;
    background-color: black;
    color: white;
    padding: 1.2dvh;
    width: 90%;
    cursor: pointer;
`;
